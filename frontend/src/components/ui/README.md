# UI Components Directory

This directory contains reusable, primitive UI components managed by **shadcn/ui**.

Components here are styled with Tailwind CSS, support dark mode natively, and are fully accessible via Radix UI primitives.

To add new components, use the shadcn CLI:
```bash
npx shadcn@latest add [component-name]
```
