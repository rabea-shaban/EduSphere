# Styles Directory

This folder is reserved for additional global stylesheets, typography overrides, or legacy CSS integrations.

## Styling Rules

1. **Tailwind First**: All styling should be implemented using utility classes inside component files wherever possible.
2. **Global Styles**: Core design themes and CSS variables are configured in `src/app/globals.css`. Do not add styling here unless it cannot be achieved with Tailwind classes.
